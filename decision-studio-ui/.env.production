# Production: points to the deployed API.
# trydecisionstudio.com is a dormant Squarespace placeholder, unrelated to
# this app -- confirmed live 2026-08-30 (this domain doesn't even resolve).
# No custom domain is set up for the backend yet -- pointed at the real
# Railway URL until that DNS/custom-domain work happens. NOTE: Cloudflare
# Pages' build already uses this same Railway URL via its own dashboard env
# var (confirmed by inspecting the deployed bundle), which takes precedence
# over this file -- this edit is for the file's own accuracy, not a runtime
# change.
VITE_API_URL=https://agent9-hermes-production.up.railway.app

# Full debate mode in production — all 4 stages for maximum depth
VITE_DEBATE_MODE=full

# Supabase Auth — Infra B
# Set these in Cloudflare Pages environment variables dashboard.
# Get values from: Supabase Dashboard > Project Settings > API
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder-replace-in-cloudflare-pages-env

# Tenant domain → client_id mapping (comma-separated domain:client_id pairs)
# Example: apex.com:apex_lubricants,hess.com:hess
# Set in Cloudflare Pages environment variables.
VITE_TENANT_DOMAIN_MAP=
