# Documentation — docs/

## Architecture Docs: docs/architecture/

Read before designing changes to agents, registries, or workflows:

| File | Contents |
|---|---|
| `Agent9_Architecture_Overview.md` | Full system architecture, principles, components |
| `orchestration_architecture.md` | Orchestrator-driven design principles |
| `orchestrator_implementation.md` | Dependency resolution implementation detail |
| `core_workflow_diagram.md` | Visual workflow diagrams, agent interaction sequences |
| `analytical_methodology_positioning.md` | KT + MBB architecture rationale, IS NOT as control group, competitive positioning |
| `hitl_decision_philosophy.md` | HITL gate design, Solution Q&A, cognitive dissonance principle, context stack, approval flow |
| `llm_prompt_redesign_da_sf.md` | DA+SF prompt redesign proposal: structured outputs, principal/business context contract, prompt slimming (Phase 13 Cat 2/4 = Phase 15 Stages A–C) |
| `theory_layer_design.md` | Theory layer: per-client causal model + assumption register, HITL-accreted + template-seeded, provenance ladder; SF/LLM trust pillars = DEVELOPMENT_PLAN Phase 15 Stages D–F. **§5.5 status corrected 2026-08-30: the `20260723_theory_layer_causal_schema.sql` migration it described as "NOT applied to any database" had in fact been applied long since (verified directly — `schema_migrations` records it, the `assumptions` table is live, later migrations extend it). Its gating condition — a concrete producer or consumer — is now met on both sides: SF HITL pre-registers a solution's bets, VA grades them (Phase 17 T3). §7's visualization sketch is superseded in shape by `kpi_relationship_basis_design.md` §4/§5 (conditional stack, variance bridge) — see the note inline in §7.** |
| `persona_council_experiments.md` | **Experimental record + method, no build authorised.** Seven arms testing whether consulting personas ask different questions (Stage I B-3). Findings: topic selection converges under every configuration; content differentiates with roster differentiation; a better model converges weak personas and diverges genuine ones; the differentiation survives stripping the authored profiles. Contains the transferable methodology lessons (compute the null, one variable per run, predict before running, beware stacked tests) and a phased screening design. §7b adds the **evidence-scope experiment** (6 real SF runs): causal traversal depth showed no measurable effect because the *direct* edge's mechanism prose already narrates what the 2-hop node contributes — graph depth and mechanism text are substitutes — while market-signal routing produced a checkable correction (indexing a base-oil surcharge to Group I/II spot rather than to WTI crude). **Next spend is an outcome measure, not another arm**. **§7b is now load-bearing precedent (2026-08-31):** its "graph depth and mechanism prose are substitutes" finding, plus the one real effect it did measure (market signals correcting WTI -> Group I/II spot), supplied the discriminator used to REJECT threading the Phase 17 theory layer into the SF prompt -- context carrying a concept the model already has changes nothing; context correcting a fact it gets wrong helps. See `DEVELOPMENT_PLAN.md` Phase 17 "RESOLVED 2026-08-31" |
| `problem_framing_design.md` | **Design settled, build in progress — Phase 19.** DQ link 1 (appropriate frame) failed both the 13-run corpus adjudication and a second, structurally different problem shape (§10–11) — the only systematic cap left on the chain, empirically confirmed rather than merely argued. Mechanism: `problem_framing` becomes the **mandatory first topic** of the existing refinement interview (not an unbundled pre-DA step — that was superseded), gating "Generate Solutions" until answered; SCQA generation defers until the frame is chosen, then generates against it. Alternatives shown at 1–2 hops via the causal graph, unfiltered by direction, plus (Decision #12) a labeled market-conflict alternative sourced from Market Analysis, now wired as an input to DA's own framing/SCQA construction rather than a sidecar between DA and SF. Build sequencing: `C:\Users\Blell\.claude\plans\with-this-now-in-goofy-meteor.md`; status: `DEVELOPMENT_PLAN.md` Phase 19 |
| `decision_quality_rubric.md` | **Design note, not built.** The outcome measure Stage H/I has been missing — Stanford SDG **Decision Quality** (six links, weakest-link scoring) adopted as the referent that `persona_council_experiments.md` §5 says must precede further optimisation. Maps each DQ link to fields already present in the SF payload and to existing `src/analysis` instruments; records a **falsifiable prediction before scoring** (links 1 *frame* and 2 *alternatives* fail; 3/5/6 pass; the moderator grades only the links already passing). Corpus is the 33 options in `tools/ab_harness/scope_arm_*.json` — no new API spend — stratified pre/post the `_build_kt_summary` fix. Chosen over MAP / Vroom-Yetton / KT-DA / AHP for stated reasons: DQ is a *standard*, not a *procedure*, so it grades the artefact without asking a customer to change how they meet |
| `dq_l1_framing_signal_design.md` | **Design note, not built — half the held-out validation done, half structurally blocked (2026-08-22).** `score_run()` has no access to the framing gate's persisted decision at all; L1 infers "frame examined" purely from vocabulary-scanning SF synthesis text. The naive fix (score off `framing_choice != "confirm_stated"`) conflates "was a different objective chosen" with "were the solutions structurally widened" (L1's own stated definition) — recommends reporting both as separate signals, never collapsed. The originally-planned validation corpus (`tools/ab_harness/scope_arm_*.json`, 13 runs) turned out unable to supply one of the two signals at all — it predates the framing gate by weeks, generated via a scripted path that never persists a framing decision. Split in two: `solutions_widened` (no framing record needed) scored clean against that corpus — 13/13, 2 PASS / 11 FAIL. `objective_examined`'s divergence test is genuinely blocked, not just unscheduled — every real framing-gate run in existence was generated by this session testing its own machinery, so none qualify as independent; needs real accumulated usage over time, not more code |
| `kpi_semantic_contract.md` | **§3 (additivity/unit_class/aggregation_method) — BUILT 2026-08-30 (Phase 17 Stage 1, T1's second half): all 7 fields live on `KPI` + migration `20260830160000` + `src/registry/validators/additivity_validator.py`, seeded for lubricants. `additive_across_dimensions` defaults to None, never True. Still open: threading these facts into the SF *prompt* — today they are checked on SF's output, not supplied as generation-time input.** §4 (**sliceability**: which dimensions a KPI must *not* be cut by) is a **different claim — this doc's own "not built" framing is stale for §4.** `KPI.not_sliceable_by` is real, live, and enforced (`src/registry/models/kpi.py`, `A9_Data_Governance_Agent.check_slice_validity()`, excluded from analysis in `a9_deep_analysis_agent.py`), backfilled for all 3 real clients since 2026-08-15 — found only by checking code directly, since neither this doc's header nor the DA/DGA PRDs mention it. **§3a: generalizes §3 beyond finance** — worked example for an operational client (OEE %, on-time-delivery rate, cycle time, inventory on hand). No phase currently tracks §3 in `DEVELOPMENT_PLAN.md` (one was drafted 2026-08-30 and deleted the same day — written without first checking whether §4 was already built, which it was; see the PRD-sync discussion in that day's session) |
| `causal_edge_direction_and_magnitude_design.md` | **Direction — built and live (2026-08-20). Magnitude/curve — design note, not built.** Found live 2026-08-20: a Net Revenue framing gate offered "Cost of Goods Sold" as an alternative objective, but COGS has no real relationship to Net Revenue — the only 2-hop path walks the `gross_margin_pct↔cogs` edge *backward* (that edge's own mechanism says COGS causes margin, not the reverse), because `KPIRelationship` has no field encoding which KPI causes which — `kpi_id`/`related_kpi_id` ordering is inconsistent across the existing seed data. Proposes `causal_direction` (filtered at `_build_framing_prompt`, not the shared undirected BFS SA also uses) and `magnitude_category`/`magnitude_coefficient` (mirrors `confidence`'s existing "categorical, not a float" discipline; a real coefficient requires `provenance="va_validated"`, same guardrail pattern as `intervention_tested`) — extends `theory_layer_design.md`'s already-scheduled P1 write-back (verdict + lag) to also cover direction and magnitude, which it doesn't today. An *estimated* (not yet validated) magnitude is still useful — ranking/filtering framing alternatives by strength, and a real data-driven signal for L1 to eventually weigh instead of vocabulary-matching synthesized prose. Curve/non-linear treatment explicitly out of scope — no implementation (VA's or a hypothetical Granger one) would produce it without a materially different method |
| `data_product_time_dimension_planning.md` | **Design note, not built** (interim mitigation shipped). `dp_lubricants_sales` carries three genuinely distinct dates (order/delivery/billing) but `_resolve_time_spec` (`a9_data_product_agent.py:4385`) picks one `primary` time dimension per DATA PRODUCT with no per-KPI override anywhere in the `KPI` model. Found live: 90.2% of Sales Order Items had `delivery_date` in a different fiscal month than their revenue's actual recognition period, a 5.5–8.9% swing on period-sliced value KPIs. Shipped: Sales' primary switched to `fiscal_year_period` (matches Finance), `order_date`/`delivery_date` kept as non-primary entries. Proposed general fix (KPI-level `time_dimension_ref` + a `key` slug per time-dimension entry) explicitly deferred as a fast-follow, not pre-demo — narrow and additive, but sits on the core DPA SQL-generation path every workflow depends on |
| `reframe_relaunch_and_lineage_design.md` | **Disclosure text + VA-capture half shipped 2026-08-22; new-window relaunch + lineage not built.** Found live: reframing a KPI (e.g. Gross Margin % → COGS) doesn't re-run DA — `generate_scqa_for_frame` reconstructs KT/change-points from the *original* KPI's `da_output`, only the narrative text changes, so solutions end up dressed in the new KPI's language while grounded in the old one's evidence. Shipped: the honest disclosure banner Phase 19 spec'd but never built (`DeepFocusView.tsx`), and VA registration's `kpi_id` bug fixed — it always registered the situation's original KPI, never the framing decision's chosen one (`_resolve_va_kpi_id_and_framing()` in `workflows.py`, 6 regression tests) — plus a new `FramingSnapshot`/`target_metric` capture on `AcceptedSolution` (migration applied, verified live). Not built: the new-window entry point (`/dashboard?kpi=...&reframed_from=...`) so a reframe actually gets its own DA/KT/MA rather than re-narrated evidence; the framing gate running fresh at each hop (settled as the right answer — confirming at any hop is legitimate, not a failure); `reframed_from_id` lineage linking successive framing decisions, needed for VA to see a multi-hop chain rather than just the last hop |
| `kpi_relationship_basis_design.md` | **§2/§3/§4/§5 all BUILT 2026-08-30 (Phase 17 Stage 6).** `basis` (`accounting_identity`\|`causal_estimate`) is a real column, defaulting to `causal_estimate` so nothing silently becomes "certain" — a heuristic was tried first and would have been WRONG on live data (`product_sales_revenue↔cogs` carries NULL causal_rung+confidence exactly like the four identity edges yet is not one). §4's variance bridge is built and verified against the note's own worked example (+5.91/−10.91 → exactly −5.0pp) and live BigQuery (34.43%→29.94%, −4.48pp = net_revenue +1.95 / cogs −6.43, residual 0); its >2-input tripwire is implemented, not just noted. §5's conditional stack is the shipped exhibit shape. Original finding, for context: `KPIRelationship` treats every edge with the same vocabulary (`confidence`/`provenance`/`mechanism`) whether it's a genuinely uncertain causal claim or an accounting identity true by construction — found via the waterfall-chart question on the Framing Evidence Map mockup. Confirmed three misclassified edges in `scripts/clients/lubricants.py`: `net_revenue↔gross_margin_pct` and `gross_margin_pct↔cogs` are pure arithmetic (margin% is calculated from revenue and COGS), and `base_oil_cost→cogs`/`distribution_cost→cogs` are exact `account_category` components of COGS's own `account_type` bucket, confirmed via their own SQL — their mechanism text ("inventory lag," "spot rates") actually describes a different, one-hop-removed external causal claim these edges don't encode. Proposes a `basis: accounting_identity \| causal_estimate` field — determines chart rendering (waterfall vs. causal-graph node) and which edges Phase 17's Core Spine can draw on sooner than that phase's blanket "not built" audit assumed, since FI KPIs' existing `account_type`/`account_category` filters are already an informal, correctly-summing decomposition |
| `ui_refinement_plan.md` | **Audit complete, most fixes not yet built.** Compliance-and-cleanup pass against the design language already established in `ui_brand_guidelines.md`/`decision-studio-ui/DESIGN_SYSTEM.md` — not a redesign. Confirms real compliance where it exists (no drill-down/filter controls in any visualization, Answer-first SCQA already shipped) and catalogs concrete gaps: hardcoded severity colors in 15+ files instead of the `severity-*` token group (DESIGN_SYSTEM.md's own "single most important rule"), no client indicator in `AppHeader`, an open question on Variance Breakdown disclosure depth, cross-references the Settings tab bar density item. **Extended 2026-08-22** with §7 (review of generic external "executive dashboard" advice against decisions this codebase actually made — light canvas and a global ask-anything bar rejected with reasons, an accessibility theme toggle and a scoped chat entry point accepted, and a firm product-scope decision recorded: **no forecasting or scenario-planning functionality for now**) and §8 (defects found driving the live app: `/portfolio` renders a raw developer error on direct navigation, `Test Probe` is a selectable login identity, severity colour fights sparkline colour on every KPI tile, `ANALYZE` is hover-gated, a stray `0` renders from a `\|\|`-chain truthiness bug at `ProblemRefinementChat.tsx:451`, Variance Breakdown repeats one total across all seven dimension rows, and a latent synthetic-sparkline fallback that is verified not firing but would be undetectable if it did). §8.6 files the SA/DA/SF-quality findings to their real owners rather than leaving them as "UI polish". §4.4 corrected as stale; Tier order rewritten to put demo-blockers first |
| `decision_framer_and_decision_maker_personas_design.md` | **Design note, not built — three open questions need sign-off.** Every principal today runs the identical `DashboardView → DeepFocusView → refinement → debate → briefing` pipeline: `DecisionStudio.tsx:63-136` branches only on whether a situation is selected, never on role, and `PrincipalProfile` has no field distinguishing a steward who frames problems from an executive who signs off. Observed live: running as CEO, the framing gate said "Owned by Finance Manager. You may still submit" — the system knew the KPI belonged to someone else and still asked the CEO to do the interview. Independently pre-derived in `raci_accountability_model.md`'s "designed for the wrong persona". Proposes a first-class `workflow_role` enum defaulting to `framer` (non-breaking), explicitly **not** another `metadata` string — `decision_style` is the cautionary precedent, written by the frontend as if first-class but read out of `metadata` and defaulting to "Analytical" for effectively every production principal. Must preserve M1 (identical facts and recommendation for all principals, stated in three independent places); the split is a **workflow-stage** axis, not a content axis. Requires reconciling `hitl_decision_philosophy.md` §6's "single principal as decision maker", already de facto contradicted by PIB's shipped delegation |
| `executive_briefing_redesign.md` | **Design note + published mockup, not built.** Grounded in a full live SF run (2026-08-22, EBITDA / Base Oil & Additives; payload and screenshots in `decision-studio-ui/scratchpad/sf_run2/`). The briefing's own `unresolved_tensions[0]` stated that two of its three options rest on contradictory hypotheses — "both cannot be the dominant driver" — from a collapsed section at the bottom of a 10-accordion page. That contradiction *is* the decision: not "pick one of three" but "we cannot yet tell which hypothesis is true, and the recommended first action finds out." Also documented: opt_1 and opt_2 modelled at an identical `$3.8–5.2M` recovery while differing sharply on speed and reversibility (opt_2 strictly dominated, invisible as a table row); `under opt_1` rendered inside the decision ask; Cost of Inaction leading with a projected *level* beside the actual erosion, an order of magnitude apart in the page's most prominent block; an 86-word opening sentence; and a fixed-height inner scroll pane that prevents whole-page capture (verify Print/Export before trusting it as the meeting artifact). Design is **one document, two default disclosure states** — which satisfies M1 rather than fighting it. The framer layer needs no new backend fields: `moderator_grades` already carries constraint survival, causal grounding with lag, and arithmetic consistency, i.e. a trust ledger buried in a bottom accordion |
| `collapsible_left_nav_design.md` | **Design note, not built — smallest of the current UI threads.** Navigation is fragmented per-page with no shared mechanism: `AppHeader` is rendered only on `/dashboard` and offers two icon links; `/context` and `/portfolio` are dead ends with respect to each other; `/portfolio` on direct navigation renders a raw developer error naming an internal ID format. Corrects a stale assumption carried in both `DEVELOPMENT_PLAN.md:119` and `ui_refinement_plan.md` §4.4 — the Settings left-nav refactor **already shipped** as `SettingsLayout.tsx`; what remains is that it is not collapsible, is Settings-only, and that three competing group taxonomies now exist. Scope is 4–6 authenticated sections, not 15. Needs a width-collapse (icon rail) pattern with no precedent in the codebase — `DESIGN_SYSTEM.md` documents only content-collapse accordions and has no breakpoint conventions at all |
| `refinement_iteration_and_session_persistence_design.md` | **Design note, not built.** Three needs at the refinement→SF seam: pause/resume (the refine endpoint is stateless by explicit design — `workflows.py:376-380` — so a refresh loses the entire transcript and restarts at turn 0); a deliberate **second refinement round** after seeing the solution set; and not silently destroying prior work. Live bug documented with file:line: re-opening refinement for an already-solved situation makes `CouncilDebatePage.runDebate()` delete every `solutions_*`/`briefing_*`/`solution_request_*` key and re-run **both** Stage 1 and synthesis (~5.5 min of real LLM spend, measured) with no confirmation and no partial-retry path — because `DeepFocusView` has no idea a debate already completed. Proposes `RefinementRound` with `iterated_from_id`, deliberately mirroring `reframed_from_id` in `reframe_relaunch_and_lineage_design.md`: **same lineage mechanism, different axis** (same objective revised vs. a different objective), which must compose rather than compete. Notes why onboarding's day-based resume pattern does *not* transfer — that works because each step already writes a durable registry row, and DA/SF's intermediate steps write nothing |
| `audit_event_system_design.md` | **Design note, not built. Supersedes the error-log / audit-log portions of Infra A5 and Infra C.** Nothing today persists backend errors or agent audit signals: no `audit_log`/`event_log`/`error_log` table in any of 34 migrations, no endpoint, no UI, and logs go to stdout only — `A9_SharedLogger` is named as the target in both CLAUDE.md files and does not exist anywhere in `src/`. SF's `audit_log` is a **request-scoped local variable** (`a9_solution_finder_agent.py:1028`) discarded when the response completes; a real run on 2026-08-22 emitted six genuinely useful operational events and threw all of them away, and even in-process consumers treat it as unreliable (`mechanism.py:140-145` keeps a title-matching fallback). Proposes an RLS-scoped `audit_events` table (the mandatory GRANT/ENABLE/POLICY trio plus `_RLS_TABLES` registration), a typed write path piloted on SF's existing events, an admin query API reusing `admin.py`'s `X-Admin-Key` gate, and a diagnostics page kept separate from `ConnectionHealthPanel` (connectivity and application diagnostics are different questions). Deliberately scoped narrower than Infra C — sign-in audit and archive lifecycle left for their own design. Would unblock `dq_l1_framing_signal_design.md`, explicitly stalled on "real accumulated usage over time" |
| `strategic_causal_graph_design.md` | **Design note, not built.** DQ link 1 (frame) failed 3/3 live runs on 2026-08-19 (2 MBB, 1 lens), identical reasoning each time. Reframed against `theory_layer_design.md` §2.2's own three layers (physical/operational, economic, commitment): today's causal graph is entirely layer-2 — the real problem is that it cannot represent an alternative outside layer 2 at all. Two concrete gaps, deliberately **scoped apart, not one initiative** (2026-08-20 boil-the-ocean check): **layer 3 (commitment) is the near-term recommendation** — reframes a constraint the system already curates and trusts (the price-lock covenant already in "Known Constraints") as something a decision can target, not just obey; zero new data collection, dependable today. **Layer 1 (physical/operational) is explicitly deferred** — dependable in principle via SAP MM (inventory/materials — checked live, nothing SD/MM-related exists in this codebase today, only FI), but a real connector build comparable in scope to the already-deferred MCP connectivity work; gated on a real SAP-running prospect's deal, not built speculatively. Curation resolved for whichever layer gets built: LLM-drafted candidates (same `A9_KPI_Assistant_Agent` shape — suggest, refine, human-finalize), grounded in real DA/MA facts, never presented live/unsupervised. Highest-value consequence either way: converts L1 from a 71%-false-positive text screen into a real check of whether a cross-layer alternative was offered and given a verdict |

Technical debt and known issue plans:

| File | Issue |
|---|---|
| `business_process_provider_initialization.md` | Provider init warnings and patterns |
| `data_governance_agent_connection.md` | Missing DG → DP connection pattern |
| `principal_id_based_lookup_plan.md` | Migration from role-based to ID-based lookup |
| `business_process_hierarchy_blueprint.md` | Future hierarchical BP design |
| `registry_display_ui_updates.md` | Decision Studio UI updates for business processes |

## Agent PRDs: docs/prd/agents/

One PRD per implemented agent. **Read the PRD before adding new capabilities to an agent.**

- `a9_orchestrator_agent_prd.md`
- `a9_principal_context_agent_prd.md`
- `a9_data_governance_agent_prd.md`
- `a9_data_product_agent_prd.md`
- `a9_situation_awareness_agent_prd.md`
- `a9_deep_analysis_agent_prd.md`
- `a9_solution_finder_agent_prd.md`
- `a9_llm_service_prd.md`
- `a9_nlp_interface_agent_prd.md`
- `a9_kpi_assistant_agent_prd.md`
- `a9_value_assurance_agent_prd.md`

## Update Rule

When adding new agent capabilities:
1. Update the relevant PRD in `docs/prd/agents/`
2. Update the agent's row in the **Current Capabilities** table in root `CLAUDE.md`

## Other Reference Docs (project root)

- `AGENT_SPECIFICATIONS.md` — extracted PRD requirements and protocol violations checklist
- `TECHNICAL_DEBT.md` — full technical debt inventory
- `DEVELOPMENT_PLAN.md` — **Active development plan** (Phase 7+: VA, Opportunity DA, Business Optimization)
- `IMPLEMENTATION_PLAN.md` / `HERMES_IMPLEMENTATION_PLAN.md` — **DEPRECATED** historical plans (Phases 1-6, completed)
